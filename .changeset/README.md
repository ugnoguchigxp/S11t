# Changesets

Add a changeset for changes that alter either public package. `s11tnext` and
`s11tnext-cli` are a fixed release group, so they stay on the same release line.

Canary builds use Changesets snapshot versions in an ephemeral checkout. Do not
enter prerelease mode on `main`.
