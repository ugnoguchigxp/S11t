schema_version = 1
[context]
id = "invalid:messages"
version = "1.0.0"
owner = "fixture"
source_locale = "en-US"
required_locales = ["en-US"]
output = "messages"
[locales."en-US"]
text = "Unsupported"
